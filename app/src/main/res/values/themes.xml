<resources>
    <!-- Our app theme extends Material Components' Material 3 theme (XML) -->
    <style name="Theme.AliveShow" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
